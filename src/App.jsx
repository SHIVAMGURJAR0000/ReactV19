import React from "react";
import { Todo } from "./projects/Todo/Todo";

export const App = () => {
  return (
    // project 2 continue
    //local storage
    //for the production run bun run build
    <section>
      <Todo />
    </section>
  );
};
